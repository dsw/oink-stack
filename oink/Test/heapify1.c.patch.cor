
---- START ---- Test/heapify1.c
Test/heapify1.c:10: param decl x2
Test/heapify1.c:12: param use x2
---- STOP ----

--- Test/heapify1.c
+++ Test/heapify1.c
@@ -14,2 +14,4 @@
-  int y2;
-  int *y2_p = &y2;              // and addr taken: yes
+  int (*y2) = xmalloc(sizeof *y2);
+  int *y2_p = &
+// xform this use
+y2;              // and addr taken: yes
@@ -18,2 +18,4 @@
-  struct Foo foo1;              // function/auto: stack
-  struct Foo *foo1_p = &foo1;   // and addr taken: yes
+  struct Foo (*foo1) = xmalloc(sizeof *foo1);              // function/auto: stack
+  struct Foo *foo1_p = &
+// xform this use
+foo1;   // and addr taken: yes
@@ -21,2 +21,4 @@
-  struct Foo foo3;              // function/auto: stack
-  int *foo3_q_p = &(foo3.q);    // and addr taken: yes
+  struct Foo (*foo3) = xmalloc(sizeof *foo3);              // function/auto: stack
+  int *foo3_q_p = &(
+// xform this use
+foo3.q);    // and addr taken: yes
